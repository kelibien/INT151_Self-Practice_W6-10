- DOMContentLoaded จะทำงานเมื่อ HTML โหลดเสร็จ แต่ยังไม่ต้องรอรูปหรือไฟล์อื่น
- เข้าใจหลักการ Event-driven Programming -> เว็บจะ “ตอบสนอง” ตามการกระทำของผู้ใช้ (เช่น คลิก พิมพ์ เคลื่อนเมาส์)
                                       -> ทำให้เว็บมีความโต้ตอบ (interactive) มากขึ้น